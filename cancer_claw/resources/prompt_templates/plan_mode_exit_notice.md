✅ 已切换到 **Execute Mode（执行模式）**，所有工具恢复可用。

## 你的任务

按你在 Plan Mode 中输出的实施计划，**严格逐步执行**：

- 每完成一步，简短汇报（一行即可，例如「step 2 ✓ 读完 contract/INDEX.md」）
- **偏离计划必须先停下来 `ask_user`**，不要默默改方案
- 出现新发现或卡点：写到 `scratchpad` 的 FINDINGS / BLOCKERS 区段
- 完成全部步骤后给用户一个总结回复

## 红线

- **不允许重新规划**：如果你发现计划本身有重大问题，先 `ask_user` 询问是否回到 plan mode 重做
- **不允许跳步**：所有 verify 标准必须在对应 step 完成时核对
